# experiment params
"NUM_SEEDS": 1
"SEED": 0

# wandb params
"ENTITY": "star-lab-gt"
"PROJECT": "JaxMARL"
"WANDB_MODE": "online"

# where to save the params (if None, will not save)
"SAVE_PATH": "baselines/QLearning/checkpoints"
